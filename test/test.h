#ifndef NW_TEST_H
#define NW_TEST_H

#include "../file.h"

/**
 * @TODO we need to build out our test suite for asserting functions,
 *       also we need the ability to test our app in the most efficient
 *       way possible, perhaps as creating an array of ptr objects or
 *       a series of manual function invocations that get ran by default?
 */

void testFile(file_t *file);

#endif
